
[EyeWitness](https://github.com/RedSiege/EyeWitness) can take the XML output from both Nmap and Nessus and create a report with screenshots of each web application present on the various ports using Selenium.

```shell
eyewitness --web -x web_discovery.xml -d inlanefreight_eyewitness
```

---
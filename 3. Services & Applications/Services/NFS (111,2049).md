
### Enumeration

```bash
nmap --script 'nfs*' <IP> -p 111,2049
### fetching info

showmount -e <IP>
### show available mounts and -e flag is for exports

use auxiliary/scanner/nfs/nfsmount
### Metasploit
```

---
### Mounting

```sh
sudo mount -t nfs 10.129.14.128:/mnt/nfsshare ./dir-to-mount-on/ -o nolock
### mounting, -t flag tells the FS

ls -n /mnt/nfs 
### fetching UIDs and GUIDs 
```


we can sometimes cp files to our machines using `sudo`.

---
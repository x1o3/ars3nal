
If PATH is writable:

```shell
export PATH=.:${PATH} ### call bin from current dir

export PATH=/tmp:${PATH} ### call binaries from temp
```

We can know build malicious binary and scripts and they will be executed instead of the real one.

---